<% if(user){ %>
  <h1>Welcome to SubTrack, <%= user.username.charAt(0).toUpperCase() + user.username.slice(1).toLowerCase(); %>
  </h1>
  <% }else{ %>
    <h1>Welcome to SubTrack, Please sign in</h1>
    <% } %>